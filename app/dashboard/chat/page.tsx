import { NewChatClient } from "@/components/projects/threads/new-chat-client";

/**
 * NewChatPage — /dashboard/chat
 *
 * Página de inicio de conversación. Renderiza el cliente que compone
 * ChatWelcome, ChatSuggestions y ChatInput.
 */
export default function NewChatPage() {
  return <NewChatClient />;
}
