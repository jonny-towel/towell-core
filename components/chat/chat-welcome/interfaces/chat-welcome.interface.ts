export interface ChatWelcomeProps {
  /** Título principal (h1). */
  title?: string;
  /** Subtítulo descriptivo. */
  subtitle?: string;
  /** Clases CSS adicionales para el contenedor. */
  className?: string;
}
